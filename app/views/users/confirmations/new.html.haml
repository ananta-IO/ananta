.page-header
  %h1 Resend confirmation instructions
.row
  .span10
    = twitter_bootstrap_form_for(resource, :as => resource_name, :url => confirmation_path(resource_name), :html => { :method => :post, :class => "form-stacked" }) do |f|
      = f.inputs do
        = f.email_field :email
      = f.actions do
        = f.submit "Resend confirmation instructions"
  .span4
    = render :partial => "users/shared/links"